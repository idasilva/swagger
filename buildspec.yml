version: 0.2

env:
  variables:
    AWS_S3_NAME: "digital-aws"
phases:
  build:
    commands:
      - echo "Entered the build phase.."
  post_build:
    commands:
      - aws s3 sync ./swagger/ s3://AWS_S3_NAME --acl public-read
